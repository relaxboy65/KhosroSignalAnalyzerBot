name: KhosroSignalAnalyzerBot

on:
  schedule:
    - cron: "*/15 * * * *"   # هر 15 دقیقه یکبار
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with: { python-version: '3.11' }
      - run: pip
